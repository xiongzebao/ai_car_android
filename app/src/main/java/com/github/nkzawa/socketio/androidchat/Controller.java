package com.github.nkzawa.socketio.androidchat;

public class Controller {

 /*   public static void turnLeft(){
        MySocketManager.getInstance().attemptSend(new MessageEvent(Constants.ACTION_TURN_LEFT));
    }

    public static void turnRight(){
        MySocketManager.getInstance().attemptSend(new MessageEvent(Constants.ACTION_TURN_RIGHT));
    }

    public static void forward(){
        MySocketManager.getInstance().attemptSend(new MessageEvent(Constants.ACTION_FORWARD));
    }

    public static void back(){
        MySocketManager.getInstance().attemptSend(new MessageEvent(Constants.ACTION_BACK));
    }

    public static void stop(){
        MySocketManager.getInstance().attemptSend(new MessageEvent(Constants.ACTION_STOP));
    }

    public static void speedUp(){
        MySocketManager.getInstance().attemptSend(new MessageEvent(Constants.ACTION_SPEED_UP));
    }

    public static void speedDown(){
        MySocketManager.getInstance().attemptSend(new MessageEvent(Constants.ACTION_SPEED_DOWN));
    }

    public static void turnRound(){
        MySocketManager.getInstance().attemptSend(new MessageEvent(Constants.ACTION_TURN_ROUND));
    }
*/
}
